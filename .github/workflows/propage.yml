name: propage changement
on:
  pull_request:
jobs:
  echo_test:
    runs-on: ubuntu-latest
    if: startsWith(github.base_ref, 'bug/**')
    steps:
      - name: echo test for test
        run: echo "test"
